import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arearomainlemalin.area',
  appName: 'AREA',
  webDir: 'dist',
  loggingBehavior: 'none'
};

export default config;
